fix bug (equal maybe execute) -> vielleicht result::print implementieren aber ka

weite Zukunft:
function.exec implementieren und testen
stance -> finish (löscht die ganzen COntainer von diesem Stance vom parents Stance. Alle Konsequenzen einberechnen ist hier wichtig)

Notes für ret.execute:
	- ermittle maximum von freference.current_vstance->get_Var(name)
	- dann ret = caller->result.add(ursprünglich)
	- dann ret.add[freference.current_vstance->getVar(name)[maximum - (maximum % ursprünglich): (maximum - (maximum % ursprünglich)) + ursprünglich]]

