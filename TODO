- Access bei "=" Operation sinnvoll beachten
- Pointer und Arrays bei VarDecl hinzufügen
- ArraySubscript Operation implementieren





weite Zukunft:
IfStmntConditionHandler:
	> rechts nur variable wird zu = (umkehren hier schwer), ansonsten dump_handler
	< link nur variable wird zu = (ohne umkehren zum Glück haha), ansonsten dump_handler
	== wird zu =
	!= ignorieren
	

Pointer ueberleben:
	operation::execute if right is pointer:
		- do the operation
		- pop result
		- create result space of cpy of right
		- execute result space exals poped result

